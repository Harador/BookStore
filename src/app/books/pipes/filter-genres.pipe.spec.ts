import { FilterGenresPipe } from './filter-genres.pipe';

describe('FilterGenresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGenresPipe();
    expect(pipe).toBeTruthy();
  });
});
